package com.cjimgarten.blackjack;

import org.junit.Before;
import org.junit.Test;

import static org.junit.Assert.*;

/**
 * UserCardsTest.java
 * unit tests for UserCards class
 */
public class UserCardsTest {

    UserCards userCards;

    @Before
    public void before() {
        userCards = new UserCards();
    }

    @Test
    public void test() {
        assertTrue(true);
        assertFalse(false);
    }
}
